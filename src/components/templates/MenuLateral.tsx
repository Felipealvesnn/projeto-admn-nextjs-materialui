import { Drawer } from "@mui/material";

export default function MenuLateral() {
    return (
        <Drawer
            anchor={"left"}
            open={false}
            onClose={()=>{}}
        >
            {/* itens pra ter dentro */}
        </Drawer>
    )
}